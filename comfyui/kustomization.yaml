---
{
  apiVersion: "kustomize.config.k8s.io/v1beta1",
  kind: "Kustomization",
  namespace: "comfyui",
  # Plain Kustomize, no helmCharts: this is a simple deployment + service + route
  # (AGENTS.md). configMapGenerator keeps extra_model_paths.yaml in git; its
  # content hash in the ConfigMap name forces the deployment to pick up changes.
  resources: [
    "namespace.yaml",
    "persistentvolume.yaml",
    "persistentvolumeclaim.yaml",
    "deployment.yaml",
    "service.yaml",
    "httproute.yaml",
    "dnsendpoint.yaml",
  ],
  images: [{
    name: "docker.io/yanwk/comfyui-boot",
    newTag: "cu130-slim-v2-20260808@sha256:133df7fb7f566fab7e02d4fe7d4262b687fd658c8d75e5fafdd0cff008d742fe",
  }],
  configMapGenerator: [{
    name: "comfyui-config",
    files: [
      "extra_model_paths.yaml",
    ],
  }],
}
