apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: llama-server-tls
  namespace: llama-server
spec:
  secretName: llama-server-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - llama.k8s.junjie.pro
    - grafana.k8s.junjie.pro
