apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: headlamp
  namespace: headlamp
spec:
  parentRefs:
    - name: gateway
      namespace: gateway
  hostnames:
    - "${GATEWAY_HOST}"
  rules:
    - backendRefs:
        - name: headlamp
          port: 80
