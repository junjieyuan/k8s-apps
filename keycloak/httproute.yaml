---
{
  apiVersion: "gateway.networking.k8s.io/v1",
  kind: "HTTPRoute",
  metadata: {
    name: "keycloak",
  },
  spec: {
    parentRefs: [{
      name: "gateway",
      namespace: "gateway",
    }],
    hostnames: [
      "keycloak.junjie.pro",
    ],
    rules: [{
      backendRefs: [{
        name: "keycloak",
        port: 8080,
      }],
    }],
  },
}
