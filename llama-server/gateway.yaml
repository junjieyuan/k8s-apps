apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: llama-server
  namespace: llama-server
spec:
  gatewayClassName: cilium
  listeners:
    - name: http
      protocol: HTTP
      port: 8081
