apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: llama-server
  namespace: llama-server
spec:
  parentRefs:
    - name: gateway
      namespace: gateway
  hostnames:
    - ${GATEWAY_HOST}
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: llama-server
          port: 8080
