apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: llama-models
  namespace: llama-server
spec:
  storageClassName: local-models
  accessModes:
    - ReadOnlyMany
  resources:
    requests:
      storage: 500Gi
