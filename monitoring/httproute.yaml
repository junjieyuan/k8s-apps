apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: grafana
spec:
  parentRefs:
    - name: gateway
      namespace: gateway
  hostnames:
    - grafana.k8s.junjie.pro
  rules:
    - backendRefs:
        - name: kube-prometheus-stack-grafana
          port: 80
