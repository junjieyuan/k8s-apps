apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: postgres
resources:
  - namespace.yaml
  - statefulset.yaml
  - service.yaml
images:
  - name: docker.io/library/postgres
    newTag: "18.4"
secretGenerator:
  - name: postgres-credentials
    envs:
      - .env
